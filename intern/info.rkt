#lang info

(define collection "intern")

(define deps '("base"
               "typed-racket-lib"
               "typed-racket-more"))

(define pkg-desc "Intern datatype as new subtype of Index in Typed Racket")

(define pkg-authors '(pcn))
